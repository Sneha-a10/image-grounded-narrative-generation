# Language Generation Layer

This module generates controlled narratives from image captions using:

- Local LLM (Mistral via Ollama)
- Preset-driven constraint system
- Structured JSON output

## Features
- Semantic signal extraction
- Constraint-based story generation
- Deterministic validation (word count correction)

## Run
python main.py